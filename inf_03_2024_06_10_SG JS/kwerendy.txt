SELECT pensja, COUNT(*) 
FROM pracownicy 
GROUP BY pensja 
ORDER BY pensja DESC

SELECT imie, nazwisko, nazwa 
FROM pracownicy, stanowiska
WHERE pracownicy.stanowiska_id=stanowiska.id AND staz > 10;

SELECT nazwisko, pensja FROM pracownicy WHERE staz BETWEEN 10 AND 20

ALTER TABLE stanowiska ADD minPensja int;